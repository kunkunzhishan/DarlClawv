Resolve capability request and return JSON only.

attempt: {{attempt}}

runtime_root: {{runtime_root}}

config_skills_root: {{config_skills_root}}

codex_home: {{codex_home}}

recommended_sources: {{recommended_sources_json}}

request: {{request_json}}

Install or update the capability as a Skill under `config_skills_root/<capability_id>`.
Return CAPABILITY_READY.skill_path pointing to that config skill directory.

{{feedback_section}}
